{
  "name": "elektrosimulator-android",
  "version": "1.0.0",
  "private": true,
  "description": "Elektrosimulator Android App (Capacitor)",
  "scripts": {
    "prepare-web": "bash build-web.sh",
    "sync": "npm run prepare-web && npx cap sync android",
    "build-debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "build-release": "npm run sync && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
